---
import Layout from "@/layouts/main.astro"
import ProjectPreview from "@/components/ProjectPreview.astro"
import CTA from "@/components/CTA.astro"
import SectionHeader from "@/components/SectionHeader.astro"
import Separator from "@/components/ui/separator.astro"
import { getProject } from "@/data/projects"

const selectedWork = [
  getProject("wheely-weather")!,
  getProject("ncga-stationery-templates")!,
  getProject("creative-printing-order-flow")!,
]

const projectImpacts: Record<string, string> = {
  "ncga-stationery-templates":
    "Replaced fragile per-legislator files with one controlled workflow covering all 170 members.",
  "wheely-weather":
    "Turned weather data into a verdict-first interface so cyclists see ride conditions at a glance instead of parsing raw forecasts.",
  "creative-printing-order-flow":
    "Replaced open-ended service browsing with a grid that routes customers straight to the right order form.",
}
---

<Layout>
  <div class="content-shell pt-4 pb-12 sm:pb-16">
    <section class="pb-10 md:pb-14">
      <h1 class="max-w-3xl text-xl leading-relaxed text-foreground">
        I’m a Design Engineer who works in code. I ship product interfaces,
        internal tools, and production systems where decision clarity and detail
        both matter.
      </h1>
      <p class="mt-5 max-w-2xl text-base leading-relaxed text-muted-foreground">
        I take projects from product and interaction direction through front-end
        implementation, with a focus on usable systems that hold up in
        production.
      </p>
    </section>
    <Separator />

    <section class="py-12" aria-labelledby="work-title">
      <SectionHeader
        titleId="work-title"
        title="Selected work"
        description="Case studies and shipped work across product UI, internal tooling, information architecture, and variable-data systems."
      />

      <div class="mt-10 grid grid-cols-1 gap-y-14">
        {
          selectedWork.map((project, index) => (
            <a
              href={`/projects/${project.slug}`}
              aria-label={`View ${project.title}`}
              class="group block rounded-sm focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-4 focus-visible:outline-none"
            >
              <div class="mb-5 flex items-start justify-between gap-4">
                <div>
                  <h3 class="text-xl font-bold group-hover:underline">
                    {project.title}
                  </h3>
                  <p class="mt-2 max-w-prose text-sm text-muted-foreground">
                    {project.teaser ?? project.summary}
                  </p>
                  <p class="mt-2 max-w-prose text-sm text-foreground">
                    <span class="font-bold">Outcome:</span>{" "}
                    {projectImpacts[project.slug]}
                  </p>
                </div>
                <span class="shrink-0 text-sm text-muted-foreground tabular-nums">
                  {project.year}
                </span>
              </div>
              <ProjectPreview
                project={project}
                widths={[600, 1000]}
                sizes="(min-width: 768px) 768px, 100vw"
                priority={index === 0}
              />
            </a>
          ))
        }
      </div>

      <div class="mt-12 flex flex-wrap items-baseline gap-x-6 gap-y-3">
        <a
          href="/projects"
          class="subtle-link font-normal"
        >
          All work →
        </a>
        <a
          href="https://github.com/noahzm/nm.works"
          target="_blank"
          rel="noreferrer"
          class="subtle-link"
        >
          View this site’s source on GitHub
        </a>
      </div>
    </section>

    <CTA
      description="Open to Design Engineer roles where I can take an idea from product concept to working front-end software."
    />
  </div>
</Layout>
