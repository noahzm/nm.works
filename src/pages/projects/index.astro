---
import Layout from "@/layouts/main.astro"
import ProjectPreview from "@/components/ProjectPreview.astro"
import ProjectDisciplines from "@/components/ProjectDisciplines.astro"
import SectionHeader from "@/components/SectionHeader.astro"
import CTA from "@/components/CTA.astro"
import Badge from "@/components/ui/badge.astro"
import Separator from "@/components/ui/separator.astro"
import {
  publishedCaseStudies,
  upcomingCaseStudies,
  visualWork,
} from "@/data/projects"
---

<Layout
  title="Projects · Noah Michaels"
  description="Product design case studies by Noah Michaels: product UX, information architecture, print systems, and shipped front-end work."
>
  <div class="content-shell pt-4 pb-14">
    <header class="max-w-2xl pb-10">
      <h1 class="section-title sm:text-3xl">Work</h1>
      <p class="mt-4 text-base leading-relaxed text-muted-foreground">
        Product UX, internal tools, and print production. Each case study covers
        one decision and what it changed downstream.
      </p>
    </header>

    <section aria-labelledby="case-studies-title">
      <SectionHeader titleId="case-studies-title" title="Case studies" />
      <div class="mt-8 divide-y">
        {
          publishedCaseStudies.map((project) => (
            <a
              href={`/projects/${project.slug}`}
              aria-label={`View ${project.title}`}
              class="group project-listing-link"
            >
              <ProjectPreview
                project={project}
                widths={[360, 600]}
                sizes="(min-width: 768px) 224px, 100vw"
              />
              <div>
                <div class="mb-3 flex flex-wrap items-center gap-x-3 gap-y-2">
                  <ProjectDisciplines
                    disciplines={project.disciplines}
                    label={`${project.title} disciplines`}
                  />
                </div>
                <h3 class="text-xl font-bold group-hover:underline">
                  {project.title}
                </h3>
                <p class="mt-3 max-w-2xl text-sm text-muted-foreground">
                  {project.teaser ?? project.summary}
                </p>
              </div>
            </a>
          ))
        }
      </div>
    </section>

    {
      upcomingCaseStudies.length > 0 && (
        <section class="mt-14 pt-12" aria-labelledby="upcoming-title">
          <Separator class="-mt-12 mb-12" />
          <SectionHeader
            titleId="upcoming-title"
            title="In progress"
            description="More case studies are on the way. Everything published here shipped."
          />
          <div class="mt-8 divide-y">
            {upcomingCaseStudies.map((project) => (
              <article class="grid gap-6 py-8 md:grid-cols-[14rem_1fr]">
                <ProjectPreview
                  project={project}
                  widths={[360, 600]}
                  sizes="(min-width: 768px) 224px, 100vw"
                />
                <div>
                  <div class="mb-3 flex flex-wrap items-center gap-x-3 gap-y-2">
                    <ProjectDisciplines
                      disciplines={project.disciplines}
                      label={`${project.title} disciplines`}
                    />
                    <Badge variant="outline" class="text-muted-foreground">
                      Coming soon
                    </Badge>
                  </div>
                  <h3 class="text-xl font-bold">{project.title}</h3>
                  <p class="mt-3 max-w-2xl text-sm text-muted-foreground">
                    {project.teaser ?? project.summary}
                  </p>
                </div>
              </article>
            ))}
          </div>
        </section>
      )
    }

    <section class="mt-14 pt-12" aria-labelledby="visual-work-title">
      <Separator class="-mt-12 mb-12" />
      <SectionHeader titleId="visual-work-title" title="Selected visual work" />
      <div class="mt-8 divide-y">
        {
          visualWork.map((project) => (
            <a
              href={`/projects/${project.slug}`}
              aria-label={`View ${project.title}`}
              class="group project-listing-link"
            >
              <ProjectPreview
                project={project}
                widths={[360, 600]}
                sizes="(min-width: 768px) 224px, 100vw"
              />
              <div>
                <div class="mb-3 flex flex-wrap items-center gap-x-3 gap-y-2">
                  <ProjectDisciplines
                    disciplines={project.disciplines}
                    label={`${project.title} disciplines`}
                  />
                </div>
                <h3 class="text-xl font-bold group-hover:underline">
                  {project.title}
                </h3>
                <p class="mt-3 max-w-2xl text-sm text-muted-foreground">
                  {project.teaser ?? project.summary}
                </p>
              </div>
            </a>
          ))
        }
      </div>
    </section>

    <CTA
      description="Open to UX, product, graphic design, and prepress roles. If something here lines up with what your team needs, I’d like to hear about it."
    />
  </div>
</Layout>
